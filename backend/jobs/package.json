{
  "name": "jobs",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc -b",
    "start": "functions-framework --source=build/payArtists.js --target=payArtists",
    "dev": "concurrently \"tsc -b -w\" \"nodemon --watch ./build/ --exec npm run start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.3",
    "alchemy-sdk": "^2.2.0",
    "concurrently": "^7.5.0",
    "ethers": "^5.7.2",
    "nodemon": "^2.0.20",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.1.2",
    "dotenv": "^16.0.3"
  }
}
