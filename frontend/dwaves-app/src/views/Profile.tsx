import "styles/Explorer.scss";
import {PopularSongOfArtist} from "../components";

export const Profile = () => {
    return (
        <div className={'w-full'}>
            <PopularSongOfArtist/>
        </div>
    )
};
